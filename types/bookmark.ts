export type Bookmark = {
    id: string
    title: string
    description: string
    url: string
    image_url: string
    sitename: string
    domain: string
}

export type BookmarkCollection = {
    id: string
    title: string
}
